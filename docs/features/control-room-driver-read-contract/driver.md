---
driver_version: 1
generated_at: 2026-07-13T06:53:00Z
generated_by: work-driver-prep
source:
  project: ship
  phase: control-room-driver-read-contract
repo: ship
repo_url: https://github.com/itsHabib/ship
branch_prefix: control-room-contract-
default_runtime: local

batches:
  - id: 1
    label: driver discovery contract
    depends_on: []
    status: done
    streams:
      - task_id: tsk_01KXCY4CK4MB41XDFQR35FCKZ5
        task_slug: control-room-driver-list-contract
        spec_path: docs/features/control-room-driver-read-contract/phases/control-room-driver-list-contract.md
        branch_name: control-room-contract-driver-list
        runtime: local
        model: sonnet
        effort: extra
        touches: [packages/driver/src/service.ts, packages/cli/src/commands/driver.ts, packages/cli/src/format.ts, packages/cli/test/driver-command.test.ts]
        status: done
        pr_number: 193
        merge_commit: 66d1f49bd526632d03873ad887243eb3199ae3da
        merged_at: 2026-07-13T08:16:08Z
  - id: 2
    label: workflow observability contract after CLI/schema surface settles
    depends_on: [1]
    status: done
    streams:
      - task_id: tsk_01KXCYSQ6BACMN4E8PY6XJ8PQW
        task_slug: control-room-workflow-observability-contract
        spec_path: docs/features/control-room-driver-read-contract/phases/control-room-workflow-observability-contract.md
        branch_name: control-room-contract-workflow-observability
        runtime: local
        model: sonnet
        effort: extra
        touches: [packages/store/src/cursor-runs.ts, packages/store/src/store.ts, packages/core/src/service.ts, packages/mcp/src/mcp.ts, packages/cli/src/commands/list.ts, packages/cli/src/commands/status.ts, packages/cli/src/format.ts, packages/cli/test/list-command.test.ts, packages/cli/test/status-command.test.ts]
        status: done
        pr_number: 194
        merge_commit: e76a8dd40514e8ac0ecc483b7bbe64085aecc6ec
        merged_at: 2026-07-13T09:03:37Z

conflict_notes:
  - kind: file_overlap
    file: packages/cli/src/format.ts
    tasks: [control-room-driver-list-contract, control-room-workflow-observability-contract]
    note: "Both add public JSON/text projections and formatter tests; serialize to keep the second implementation and review based on the settled first contract."
  - kind: semantic_overlap
    file: packages/cli and shared owner schemas
    tasks: [control-room-driver-list-contract, control-room-workflow-observability-contract]
    note: "No task depends semantically on the other, but both establish additive read-contract conventions. Conservative two-batch ordering avoids conflicting schema/CLI decisions."
---

# Portfolio Control Room Ship contracts — driver manifest

Generated by `/work-driver-prep project:ship:phase:control-room-driver-read-contract` at 2026-07-13T06:53:00Z.
Consumed by `/work-driver docs/features/control-room-driver-read-contract/driver.md`.

## Resolved tasks

1. `control-room-driver-list-contract` — expose bounded, newest-first, read-only durable driver discovery through `ship driver list --json`.
2. `control-room-workflow-observability-contract` — normalize additive runtime/provider/model/duration/failure/evidence fields through workflow list and status JSON.

## Conflict scan

- File overlap: both streams touch `packages/cli/src/format.ts` and adjacent public JSON-contract tests.
- Dependency signals: none in the Dossier task bodies; driver discovery and workflow enrichment are logically independent.
- No dependency found between the driver service/store surface and the workflow owner projection.

The overlap is conservative rather than proven textual collision. Two ordered local batches keep each owner contract independently reviewable and let batch 2 reuse conventions settled in batch 1.

## Batches

1. **Driver discovery contract — local, sonnet/extra.** Small service/CLI projection with strong public-schema, mutation-sentinel, and cross-process tests. Extra effort is retained for the Windows/Linux two-process harness and negative no-mutation proof.
2. **Workflow observability contract — local, sonnet/extra.** Runs after batch 1 because it shares the CLI formatter/schema surface; adds a bounded batched store projection and covers local/cloud/rooms fixtures, no-artifact-I/O proof, redaction, and backward compatibility. Extra effort is retained for the cross-runtime owner-model work and compatibility matrix.

Both streams are local-runtime candidates: they are single-repo, test-driven TypeScript changes with no browser or external-service dependency. The specs and this manifest must merge to `origin/main` before dispatch so both worktrees inherit their contracts.

## Invocation

`/work-driver docs/features/control-room-driver-read-contract/driver.md`

For operator-paced execution, run the same command with `--batch 1`, merge it, then `--batch 2`.

## Execution result

- Driver run `drv_01KXD516QZ6506S8RV7R83SAPB` completed both serialized batches.
- Batch 1 merged as PR #193 at `66d1f49bd526632d03873ad887243eb3199ae3da`.
- Batch 2 merged as PR #194 at `e76a8dd40514e8ac0ecc483b7bbe64085aecc6ec`.
- Both batches passed local `make check`, Ubuntu/Windows CI, canonical review reconciliation, and coordinator GO.
