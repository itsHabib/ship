---
driver_version: 1
generated_at: 2026-05-31T22:45:00Z
generated_by: work-driver-prep
source:
  project: ship
  phase: ship-hardening
repo: ship
repo_url: https://github.com/itsHabib/ship
branch_prefix: ship-hardening-
default_runtime: local

batches:
  - id: 1
    label: ready now (parallel-safe)
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KSZQEFV0CWY5ZVG5NZFYT4MY
        task_slug: startup-schema-skew-guard
        spec_path: docs/features/ship-hardening/startup-schema-skew-guard.md
        branch_name: ship-hardening-schema-skew-guard
        runtime: local
        touches: [packages/store/src/store.ts, packages/store/src/migrations.ts, packages/store/src/errors.ts, packages/store/src/migrations.test.ts]
        status: pending
      - task_id: tsk_01KSZQEKQCMRVVN2GFRXH9P1JX
        task_slug: cloud-runs-make-ci-coverage
        spec_path: docs/features/ship-hardening/coverage-in-make-check.md
        branch_name: ship-hardening-coverage-in-check
        runtime: local
        touches: [Makefile, .github/workflows/ci.yml]
        status: pending

conflict_notes:
  - kind: none
    note: "Disjoint file sets (store package vs Makefile/CI) and no dep edges — fully parallel-safe."

runtime_notes:
  - "Both default to local (single-repo L1/L2 work, no cloud signal). Override to cloud per-stream if you'd rather dogfood — but the make-check task touches the Makefile/CI the cloud VM also uses, so local is the cleaner check."
---

# ship-hardening driver manifest

Generated by `/work-driver-prep tsk_01KSZQEFV0CWY5ZVG5NZFYT4MY tsk_01KSZQEKQCMRVVN2GFRXH9P1JX` on 2026-05-31.
Consumed by `/work-driver docs/features/ship-hardening/driver.md`.

Two prevention follow-ups from the cloud-phase drive (#94/#95/#96), both filed after the
`make check ≠ make ci` coverage gap + the `artifacts_json` startup-skew bug surfaced.

## Batches

### Batch 1 — parallel-safe, 2 streams

- **`startup-schema-skew-guard`** → `docs/features/ship-hardening/startup-schema-skew-guard.md`
  - touches: `packages/store/src/{store,migrations,errors}.ts` + test
  - fail loud at store open when the DB schema is behind the code (the `no such column: artifacts_json` class), + the existing-DB-upgrade regression test that was missing.
- **`coverage-in-make-check`** → `docs/features/ship-hardening/coverage-in-make-check.md`
  - touches: `Makefile` (+ maybe CI)
  - fold the coverage gate into `make check` so the agent's validation command matches CI's.

Disjoint files, no dep edges → run both in parallel.
