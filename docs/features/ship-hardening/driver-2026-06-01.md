---
driver_version: 1
generated_at: 2026-06-01T14:10:00Z
generated_by: work-driver-prep
source:
  project: ship
  phases: [ship-hardening, cloud-artifact-retrieval, v2-phase-14-cloud-docpath-remote-source]
repo: ship
repo_url: https://github.com/itsHabib/ship
branch_prefix: ship-followups-
default_runtime: cloud

batches:
  - id: 1
    label: ready now (parallel-safe, cloud)
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KT1QZW3XPNF0FDYMQZAAZAAP
        task_slug: surface-failed-run-diagnostics
        spec_path: docs/features/ship-hardening/surface-failed-run-diagnostics.md
        runtime: cloud
        touches: [packages/cursor-runner/src/_shared.ts, packages/cursor-runner/src/local-runner.ts, packages/core/src/service.ts, packages/mcp/src/mcp.ts, packages/cursor-runner/src/local-runner.test.ts]
        status: pending
        # MUST merge before batch 2 (ship-store-write-contention depends on this diagnostics surface).
      - task_id: tsk_01KSZQEQSPXPAM4SABA5C96GKC
        task_slug: cloud-artifact-retrieval-hardening
        spec_path: docs/features/ship-v2/phases/cloud-artifact-retrieval-hardening.md
        runtime: cloud
        touches: [packages/core/src/artifacts/paths.ts, packages/core/src/service.ts, packages/cursor-runner/src/cloud-runner.ts]
        status: pending
      - task_id: tsk_01KSZQEWE8VPMMQ7JDJZYY1SW8
        task_slug: cloud-docpath-error-message-followups
        spec_path: docs/features/ship-v2/phases/cloud-docpath-error-message-followups.md
        runtime: cloud
        touches: [packages/core/src/errors.ts, packages/core/src/doc-source/remoteDocSource.ts]
        status: pending
  - id: 2
    label: after batch 1 (depends on surface-failed-run-diagnostics merging)
    depends_on: [1]
    status: pending
    streams:
      - task_id: tsk_01KT1R0B92J5P0QXTCWN6K6J4W
        task_slug: ship-store-write-contention
        spec_path: docs/features/ship-hardening/ship-store-write-contention.md
        runtime: cloud
        touches: [packages/store/src/db.ts, packages/cursor-runner/src/_shared.ts]
        status: pending

conflict_notes:
  - kind: file_overlap
    file: packages/core/src/service.ts
    tasks: [surface-failed-run-diagnostics (get_workflow_run view, ~L190), cloud-artifact-retrieval-hardening (artifact finalize/download, L686-950)]
    note: "Textually disjoint regions (~500 LOC apart). Cursor edits non-overlapping hunks; the later of the two PRs to merge rebases cleanly onto main. Driver rebases each later PR before merge anyway — low risk, parallel-safe."
  - kind: dep_signal
    from: ship-store-write-contention
    to: surface-failed-run-diagnostics
    reason: "Task body: a clean 'local run contention' error 'depends on surface-failed-run-diagnostics'. Acceptance references it directly. Batch 2 gated on batch 1 completing (Task 1 merged)."
  - kind: dep_note
    tasks: [surface-failed-run-diagnostics, ship-store-write-contention]
    note: "Both may touch packages/cursor-runner/src/_shared.ts (error classification). No parallel conflict — Task 2 is in batch 2, branches off main-with-Task-1, builds on it."

runtime_notes:
  - "All four streams cloud, per operator directive: Task 2 (ship-store-write-contention) IS the fix for 'database is locked under concurrent local runs'. Cloud runs don't share a local SQLite store, so firing the whole batch on cloud sidesteps the very bug while driving it. Cloud also parallelizes batch 1's 3 streams without contending on the operator's machine. Hard rule: never schedule 3+ concurrent LOCAL runs for this batch."
  - "Cloud streams: autoCreatePR true, workOnCurrentBranch false (cursor cloud picks the branch). Specs must be on origin/main before firing (cloud reads docPath from the remote branch)."

ping_gates:
  - "A PR hits review cycle 3 (3-cycle cap) — never auto-merge past it."
  - "Any PR's weighted LOC would exceed the <1000 stretch band (all four estimated amazing/<500 — flag if reality diverges)."
  - "A cursor run fails AND the on-disk work isn't cleanly salvageable (inspect git status + events.ndjson before re-firing)."
  - "A review finding whose fix changes behavior/shape (not mechanical / not a false positive) and the call is unclear."
  - "Anything ambiguous in a task's intent."
---

# ship follow-ups driver manifest (2026-06-01)

Generated by `/work-driver-prep tsk_01KT1QZW3XPNF0FDYMQZAAZAAP tsk_01KT1R0B92J5P0QXTCWN6K6J4W tsk_01KSZQEQSPXPAM4SABA5C96GKC tsk_01KSZQEWE8VPMMQ7JDJZYY1SW8` on 2026-06-01.
Consumed by `/work-driver docs/features/ship-hardening/driver-2026-06-01.md`.

Four follow-up tasks across three phases, driven together on **cloud**. The ship-hardening pair
is a dependency chain (diagnostics surface → contention fix); the two cloud tasks are independent
cycle-2/cycle-1 review deferrals from PRs #95 / #96.

## Batches

### Batch 1 — parallel-safe, 3 streams (cloud)

- **`surface-failed-run-diagnostics`** → `docs/features/ship-hardening/surface-failed-run-diagnostics.md`
  - touches: `cursor-runner/{_shared,local-runner}.ts`, `core/service.ts` (get_workflow_run view), `mcp/mcp.ts` (output schema)
  - stop discarding SDK failure diagnostics; fold RunStatus + last error-bearing event into `errorMessage`; expose duration-vs-cap + SDK terminal status on `get_workflow_run`.
  - **Must merge before Batch 2.**
- **`cloud-artifact-retrieval-hardening`** → `docs/features/ship-v2/phases/cloud-artifact-retrieval-hardening.md`
  - touches: `core/artifacts/paths.ts`, `core/service.ts` (artifact region L686-950), `cursor-runner/cloud-runner.ts`
  - symlink-intermediate containment; timeout-bound `listArtifacts` at terminal; reorder path-safety check before manifest lookup.
- **`cloud-docpath-error-message-followups`** → `docs/features/ship-v2/phases/cloud-docpath-error-message-followups.md`
  - touches: `core/errors.ts` (DocNotFoundError), `core/doc-source/remoteDocSource.ts`
  - context-split the DocNotFoundError message (no cloud guidance on local errors); drop the dead `workOnCurrentBranch` branch in `resolveRef`.

> service.ts overlap (diagnostics view ~L190 vs artifact region L686-950) is textually disjoint — rebase whichever of these two merges second onto main before merge. Driver does this for every later PR anyway.

### Batch 2 — serial, 1 stream (cloud), after Batch 1

- **`ship-store-write-contention`** → `docs/features/ship-hardening/ship-store-write-contention.md`
  - touches: `store/db.ts` (busy_timeout + WAL), maybe `cursor-runner/_shared.ts` (contention error classification)
  - fix `database is locked` under concurrent local runs: WAL + busy_timeout backoff; surface a clear contention error (needs Batch 1's diagnostics surface); document/enforce safe local parallelism.
  - **Gated on `surface-failed-run-diagnostics` being merged** so the clean contention error has a surface to land on.

## Invocation

```
# Single command — driver walks both batches in dep order:
/work-driver docs/features/ship-hardening/driver-2026-06-01.md

# Or batch-by-batch:
/work-driver docs/features/ship-hardening/driver-2026-06-01.md --batch 1
/work-driver docs/features/ship-hardening/driver-2026-06-01.md --batch 2
```

## Run outcome (2026-06-01) — complete

All four tasks merged to `main` (all run on cloud; specs landed via #101). Each PR
went through review cycles (Copilot + @codex + @claude); every actionable finding
was addressed. Dossier tasks closed with merge SHAs linked.

| Batch | Task | PR | Merge SHA | Notable review fix |
|---|---|---|---|---|
| 1 | surface-failed-run-diagnostics | [#103](https://github.com/itsHabib/ship/pull/103) | `5be04a2` | cursor HIGH: prefer tool_call detail over trailing `status:ERROR` |
| 1 | cloud-artifact-retrieval-hardening | [#104](https://github.com/itsHabib/ship/pull/104) | `b670b5c` | codex P2: reject dangling-symlink artifact paths (`ShipFs.lstat`) |
| 1 | cloud-docpath-error-message-followups | [#102](https://github.com/itsHabib/ship/pull/102) | `c76fc04` | null-guard deleted-fork PR `head.repo` |
| 2 | ship-store-write-contention | [#105](https://github.com/itsHabib/ship/pull/105) | `c11cd2d` | codex P2: `busy_timeout` before WAL pragma; merged at 3-cycle cap w/ operator approval |

Batch 2 gated correctly on Batch 1: #105's SDK-path contention hint builds on
#103's diagnostics surface. Ran all-cloud to avoid the very local-SQLite
contention #105 fixes.
