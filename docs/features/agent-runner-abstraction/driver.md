---
driver_version: 1
generated_at: 2026-06-20
generated_by: work-driver-prep
source:
  project: ship
  phase: agent-runner-seam-neutralize
repo: ship
repo_url: https://github.com/itsHabib/ship
branch_prefix: agent-runner-
default_runtime: local

batches:
  - id: 1
    label: ready now — the foundational rename + extract
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KVH5KD73DXX611NFQKR0JBXE
        task_slug: agent-runner-seam-extract
        spec_path: docs/features/agent-runner-abstraction/phases/seam-extract.md
        branch_name: agent-runner-seam-extract
        runtime: local
        touches:
          - packages/agent-runner/src/*          # new package
          - packages/cursor-runner/src/runner.ts
          - packages/cursor-runner/src/_shared.ts
          - packages/cursor-runner/src/classify-failure.ts
          - packages/cursor-runner/src/local-runner.ts
          - packages/cursor-runner/src/cloud-runner.ts
          - packages/cursor-runner/src/room-runner.ts
          - packages/cursor-runner/src/index.ts
          - packages/core/src/service.ts          # import-name shifts only
          - packages/store/src/cursor-runs.ts     # import-name shifts only
          - packages/mcp/src/mcp.ts               # import-name shifts only
        status: pending

  - id: 2
    label: after batch 1 — rebase on the renamed seam
    depends_on: [1]
    status: pending
    streams:
      - task_id: tsk_01KVH5KMN93QFVRM5ZQH4N4XJW
        task_slug: agent-runner-decursor-identity
        spec_path: docs/features/agent-runner-abstraction/phases/decursor-identity.md
        branch_name: agent-runner-decursor-identity
        runtime: local
        touches:
          - packages/workflow/src/workflow.ts
          - packages/mcp/src/mcp.ts
          - packages/core/src/service.ts
          - packages/core/src/artifacts/prompt-template.ts
          - packages/store/src/cursor-runs.ts
        status: pending

conflict_notes:
  - kind: file_overlap
    file: packages/core/src/service.ts
    tasks: [agent-runner-seam-extract (type-name references throughout), agent-runner-decursor-identity (getRun enrichment + agent-not-created sentinel + trailer)]
    note: "Textually disjoint sub-regions (rename vs. enrichment logic), but 1a is a wide rename — serialize: 1a first, 1b rebases on origin/main after 1a merges. Could parallel-run with low rebase risk if the operator overrides."
  - kind: file_overlap
    file: packages/mcp/src/mcp.ts
    tasks: [agent-runner-seam-extract (runner type-import shift, if any), agent-runner-decursor-identity (get_workflow_run output schema +agentId/+provider)]
    note: "1a's touch on mcp.ts is import-only and may be nil; 1b owns the schema change. Subsumed by the service.ts serialization."
  - kind: file_overlap
    file: packages/store/src/cursor-runs.ts
    tasks: [agent-runner-seam-extract (ModelSelection import-name shift), agent-runner-decursor-identity (provider column)]
    note: "Disjoint (import line vs. column + persistence). Subsumed by the serialization."
---

# Agent runner abstraction — Phase 1 (seam neutralization) driver manifest

Generated by `/work-driver-prep project:ship phase:agent-runner-seam-neutralize` on 2026-06-20.
Consumed by `/work-driver docs/features/agent-runner-abstraction/driver.md`.

Design: [spec.md](spec.md) (PR #145). Both streams preserve **zero cursor behavior change (additive only)**; Phase 1's validation gate (full `make check` green ubuntu+windows + the projection-equivalence test over real golden fixtures) must pass before Phase 2 builds on the renamed seam.

## Batches

**Batch 1 — ready now, 1 stream (runtime: local)**
- `agent-runner-seam-extract` → [phases/seam-extract.md](phases/seam-extract.md) — rename `CursorRunner`→`AgentRunner`, extract `@ship/agent-runner` (interface, state machine, classification policy, fake, `EventProjection` incl. `toolCallId` + neutral config types); `@ship/cursor-runner` supplies `CursorEventProjection`. *The foundational change; lands first.*

**Batch 2 — after batch 1, 1 stream (runtime: local)**
- `agent-runner-decursor-identity` → [phases/decursor-identity.md](phases/decursor-identity.md) — provider-aware watch-URL builder; **add** `agentId`+`provider` to the MCP output (keep `cursorAgentId`); parameterize the `Co-authored-by` trailer; add the `provider` column. *Rebases on batch 1 (shared `service.ts`).*

**Why serial, not parallel:** both touch `packages/core/src/service.ts` — 1a renames runner-type references throughout it, 1b edits the `getRun` enrichment + `agent-not-created` sentinel + trailer. Sub-regions are textually disjoint, so an operator override to parallel is low-risk; the default is serialize (1a merges, 1b rebases) to avoid rebase pain on a wide rename.

**Runtime:** both `local` — single-repo, L1/L2 unit-test-driven refactors with no docker/browser/multi-repo signal. Override to `cloud` if you want to dogfood the cursor-cloud path.
