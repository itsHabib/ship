---
driver_version: 1
generated_at: 2026-06-02T04:00:00Z
generated_by: work-driver-prep
source:
  project: ship
  phase: observability-logging-foundation
repo: ship
repo_url: https://github.com/itsHabib/ship
branch_prefix: observability-
default_runtime: cloud

batches:
  - id: 1
    label: ready now (parallel-safe, cloud) — logger + classifier are disjoint packages
    depends_on: []
    status: done
    streams:
      - task_id: tsk_01KTJH8EYJF9JB8BXKRXDHC2NK
        task_slug: observability-logger-package
        spec_path: docs/features/observability/phases/observability-logger-package.md
        runtime: cloud
        touches: [packages/logger]
        status: done
        pr_number: 116
        merge_commit: 1d8c7d2
        merged_at: 2026-06-09T20:01:00Z
      - task_id: tsk_01KTJH8HWKD3DANMPJ15WE5WD9
        task_slug: observability-failure-category-enum
        spec_path: docs/features/observability/phases/observability-failure-category-enum.md
        runtime: cloud
        touches: [packages/workflow/src/workflow.ts, packages/cursor-runner/src/_shared.ts, packages/cursor-runner/src/cloud-runner.ts, packages/core/src/service.ts, packages/store/src/db.ts, packages/store/src/migrations.ts]
        status: done
        pr_number: 117
        merge_commit: f6b6f55
        merged_at: 2026-06-09T20:07:00Z
  - id: 2
    label: after batch 1 — migrate sites (needs the logger + the failureCategory enum)
    depends_on: [1]
    status: done
    streams:
      - task_id: tsk_01KTJH8S28YJPD5KR2MWET0TDH
        task_slug: observability-migrate-log-sites
        spec_path: docs/features/observability/phases/observability-migrate-log-sites.md
        runtime: cloud
        touches: [packages/store/src/db.ts, packages/store/src/store.ts, packages/cursor-runner/src/debug.ts, packages/cursor-runner/src/cloud-runner.ts, packages/cursor-runner/src/artifacts-capture.ts, packages/core/src/service.ts, packages/mcp-server/src/bin.ts]
        status: done
        pr_number: 120
        merge_commit: ff5200b
        merged_at: 2026-06-09T21:23:00Z

conflict_notes:
  - kind: none
    tasks: [observability-logger-package, observability-failure-category-enum]
    note: "Batch 1 parallel-safe: logger-package is a new leaf package (packages/logger) touching no existing source; failure-category-enum touches workflow/cursor-runner/core/store. Disjoint file sets, no dep edge."
  - kind: dep_signal
    from: observability-migrate-log-sites
    to: observability-logger-package
    reason: "Migration swaps the ~24 sites onto @ship/logger — needs the logger to exist."
  - kind: dep_signal
    from: observability-migrate-log-sites
    to: observability-failure-category-enum
    reason: "Failure log lines emit failureCategory — needs the enum + classifier. Also file-overlaps the enum task in cursor-runner/core/store, so Batch 2 rebases on Batch 1."
  - kind: file_overlap
    file: packages/cursor-runner/src/_shared.ts + packages/core/src/service.ts + packages/store/src/db.ts
    tasks: [observability-failure-category-enum (Batch 1), observability-migrate-log-sites (Batch 2)]
    note: "migrate-log-sites edits the same files the enum task touches (to swap in the logger). Gated to Batch 2 + rebased on main after Batch 1 merges — disjoint regions, clean rebase."

runtime_notes:
  - "All cloud: the root checkout is in use by a parallel rooms-backend session, so cloud keeps root untouched and avoids local-SQLite contention. Cloud parallelizes Batch 1's two streams."
  - "Cloud streams: autoCreatePR true, workOnCurrentBranch false (cursor cloud picks the branch). Specs + the locked design (docs/features/observability/spec.md) are on main, so cloud resolves docPath from origin/main."

ping_gates:
  - "A PR hits review cycle 3 (3-cycle cap) — never auto-merge past it."
  - "Any PR's weighted LOC would exceed the <1000 stretch band (enum task estimated ~365; flag if it diverges — it has a documented 2-PR split if it balloons)."
  - "A cursor run fails AND on-disk/branch work isn't cleanly salvageable."
  - "A review finding whose fix changes behavior/shape (not mechanical/false-positive) and the call is unclear — esp. the cloud-EXPIRED reclassification (a behavioral change)."
---

# Observability P1 (logging foundation) driver manifest

Generated by `/work-driver-prep project:ship:phase:observability-logging-foundation`.
Consumed by `/work-driver docs/features/observability/phases/driver.md`.

Phase 1 of the observability initiative (design locked in #111, `docs/features/observability/spec.md`).
Structured logging foundation + the shared failure-category primitive.

## Batches

### Batch 1 — parallel-safe, 2 streams (cloud)

- **`observability-logger-package`** → `phases/observability-logger-package.md`
  - new `@ship/logger` leaf package (interface + pino + config; stderr, JSON, `SHIP_LOG_LEVEL`; pino-pretty devDep, prod JSON-only).
- **`observability-failure-category-enum`** → `phases/observability-failure-category-enum.md`
  - `failureCategorySchema` (no `cancelled`, tombstone literals) + `classifyFailure` (cursor-runner, flags not cause-instanceof) + core finalize wiring (both paths) + new `failure_category` column + cloud-`EXPIRED` reclassification. Documented 2-PR split if it balloons.

> Disjoint: logger-package = new package; enum = workflow/cursor-runner/core/store. Parallel-safe.

### Batch 2 — serial, 1 stream (cloud), after Batch 1

- **`observability-migrate-log-sites`** → `phases/observability-migrate-log-sites.md`
  - swap the ~24 ad-hoc sites onto `@ship/logger`, emit `failureCategory` on failure lines; **exclude** CLI user-facing output + error-exits; extend the existing e2e stdio test for stdout purity.
  - Depends on both Batch-1 streams + file-overlaps the enum task (cursor-runner/core/store) → rebase on main after Batch 1 merges.

## Invocation

```
# Single command — driver walks both batches in dep order:
/work-driver docs/features/observability/phases/driver.md

# Or batch-by-batch:
/work-driver docs/features/observability/phases/driver.md --batch 1
/work-driver docs/features/observability/phases/driver.md --batch 2
```
