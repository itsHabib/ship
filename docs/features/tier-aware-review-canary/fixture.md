# Tier-aware review live fixture

This inert document exists only to exercise the opt-in review canary against a
real pull-request head. It carries no runtime behavior, configuration, or
credentials.

The first fixture revision proves the low-risk route. A later documentation-only
revision proves that a pushed head invalidates the earlier plan and review
evidence before the route is recomputed.

This second revision is that intentional head change; it remains inert.
