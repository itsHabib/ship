---
driver_version: 1
generated_at: 2026-07-19T06:05:00Z
generated_by: work-driver-prep
source:
  project: ship
  phase: dispatch-fallback-engine-hop
repo: ship
repo_url: https://github.com/itsHabib/ship
branch_prefix: fallback-
default_runtime: local
default_provider: cursor
default_model_id: grok-4.5

batches:
  - id: 1
    label: P2a core hop mechanism
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KXE9XW4FWYRMZY597S2VWRG3
        task_slug: fallback-engine-hop
        spec_path: docs/features/dispatch-fallback/phases/fallback-engine-hop.md
        branch_name: fallback-engine-hop
        runtime: local
        provider: cursor
        model_id: grok-4.5
        effort: extra
        touches:
          [
            packages/driver/src/engine.ts,
            packages/driver/src/escalation.ts,
            packages/driver/src/viability.ts,
            packages/store/src/driver-streams.ts,
          ]
        status: pending

conflict_notes:
  - kind: dep_signal
    from: fallback-engine-hop
    to: fallback-chain-schema
    reason: "depends_on satisfied — P1 merged as ship#210 (2f70d7a) before this manifest was generated"
---

# Dispatch-fallback P2a driver manifest

Generated by `/work-driver-prep tsk_01KXE9XW4FWYRMZY597S2VWRG3 --provider cursor --model-id grok-4.5` on 2026-07-19 (runtime switched cloud → local same day, operator call: better use of the credit).
Consumed by `/work-driver docs/features/dispatch-fallback/driver.md`.

## Batches

Batch 1 — single stream:

- `fallback-engine-hop` → [phases/fallback-engine-hop.md](phases/fallback-engine-hop.md)
  — runtime **local**, provider **cursor**, model **grok-4.5**, branch
  `fallback-engine-hop` (operator override 2026-07-18: burn the Cursor grok promo
  credit; local runtime avoids the cloud-VM flakiness while billing the same
  credit pool. The task's own recommendation was opus/extra — grok-4.5-high is the
  frontier tier of that pool, and the four-bot panel + gate sit behind it).

P2b (`fallback-hop-extras`) is NOT in this manifest — it is gated on this task's
post-merge validation gate (real bad-credential run hopping to local unattended).
