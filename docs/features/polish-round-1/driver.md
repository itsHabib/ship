---
driver_version: 1
generated_at: 2026-05-25T00:55:00Z
generated_by: work-driver-prep
source:
  project: ship
  phase: polish-round-1
repo: ship
repo_url: https://github.com/itsHabib/ship
branch_prefix: polish-1-
default_runtime: cloud

batches:
  - id: 1
    label: parallel-safe — mutation + property tracks
    depends_on: []
    status: done
    streams:
      - task_id: tsk_01KSE9N9H9K1JMV4EJXAHX4WE8
        task_slug: polish-1-mutation-track
        spec_path: docs/features/polish-round-1/polish-1-mutation-track.md
        runtime: cloud
        touches:
          - .github/workflows/ci.yml
          - .github/workflows/mutation.yml          # NEW
          - packages/cli/stryker.conf.json          # NEW
          - packages/cursor-runner/stryker.conf.json # NEW
          - packages/mcp/stryker.conf.json          # NEW
          - packages/mcp-server/stryker.conf.json   # NEW
          - packages/store/stryker.conf.json        # NEW
          - packages/workflow/stryker.conf.json     # NEW
          - README.md                               # 1-line addition only
        status: done
        pr_number: 87
        merge_commit: 0894d5054382eadb1779b37d4253f4e8b4aa8335
        merged_at: 2026-05-25T03:02:42Z
      - task_id: tsk_01KSE9NXBMRKRXZFHN68TKJS61
        task_slug: polish-1-property-track
        spec_path: docs/features/polish-round-1/polish-1-property-track.md
        runtime: cloud
        touches:
          - packages/store/src/cursor-runs.properties.test.ts        # NEW
          - packages/store/src/workflow-runs.properties.test.ts      # NEW
          - packages/cursor-runner/src/_shared.properties.test.ts    # NEW
          - packages/cursor-runner/src/cloud-runner.properties.test.ts # NEW
          - packages/workflow/src/transitions.properties.test.ts     # extend
          - packages/core/src/service.properties.test.ts             # NEW
          - packages/mcp/src/mcp.properties.test.ts                  # NEW
          # potentially packages/*/package.json if fast-check needs hoisting
        status: done
        pr_number: 86
        merge_commit: 62187f3449336c2f2e35ac86b80eb203251b2fb5
        merged_at: 2026-05-25T03:18:49Z

  - id: 2
    label: after batch 1 — README rewrite
    depends_on: [1]
    status: done
    streams:
      - task_id: tsk_01KSE9PJCQS10J2SENCC1MNBPM
        task_slug: polish-1-readme-track
        spec_path: docs/features/polish-round-1/polish-1-readme-track.md
        runtime: cloud
        touches:
          - README.md                              # rewrite (overlaps batch-1 mutation track's 1-line add)
          - packages/cli/README.md                 # NEW
          - packages/cursor-runner/README.md       # NEW
          - packages/mcp/README.md                 # NEW
          - packages/mcp-server/README.md          # NEW
          - packages/store/README.md               # NEW
          - packages/test-harness/README.md        # NEW
          - packages/core/README.md                # refresh
          - packages/workflow/README.md            # refresh
        status: done
        pr_number: 88
        merge_commit: 37706e295a337196c4cf492a29f5fa0a12f27855
        merged_at: 2026-05-25T03:39:05Z

conflict_notes:
  - kind: file_overlap
    file: README.md
    tasks: [polish-1-mutation-track, polish-1-readme-track]
    note: "Mutation track adds a single line under 'Develop' pointing at `gh workflow run mutation.yml`. README track does a full rewrite. The rewrite will subsume the 1-line addition — readme track must merge AFTER mutation track lands, picking up its line as part of the rewrite. Dep edge already enforces this order."
  - kind: dep_signal
    from: polish-1-readme-track
    to: polish-1-mutation-track
    reason: "Spec body says: 'Soft dep on polish-1-mutation-track: the top-level README's mutation-invocation line is more accurate if mutation track merges first.' Plus the file overlap above makes serialization mechanical, not just preferential."
  - kind: file_overlap
    file: packages/cursor-runner/src
    tasks: [polish-1-mutation-track, polish-1-property-track]
    note: "Mutation adds packages/cursor-runner/stryker.conf.json (package root); property adds packages/cursor-runner/src/*.properties.test.ts (src subtree). Textually disjoint — no rebase conflict. Same applies for store / workflow / mcp / mcp-server / cli where both tracks touch the same package."

runtime_notes:
  - "All 3 streams fire cloud per the operator's explicit 'run this all in the cloud using our ship tooling' directive + the post-PR-#83 /work-driver default of cloud."
  - "Cloud streams open PRs automatically via autoCreatePR=true (cursor cloud default in CloudCursorRunner). The driver doesn't need to run `gh pr create` for these."
  - "No local-force signals in any task: no Docker Compose deps, no operator-personal env vars needed during agent runtime, no multi-repo work, no localhost API deps."
---

# Polish round 1 — driver manifest

Generated by `/work-driver-prep phase: ship/polish-round-1` on 2026-05-25.
Consumed by `/work-driver docs/features/polish-round-1/driver.md`.

This manifest drives the 3-PR polish round: mutation testing expansion across the package surface + CI reorganization (track 1), fast-check property tests on every major package (track 2), and a full README refresh (track 3). All 3 fire cloud — operator's dogfood directive plus the post-PR-#83 work-driver default.

## Batches

### Batch 1 — parallel (mutation + property)

| Stream | Spec | Runtime | Touches | Why |
|---|---|---|---|---|
| `polish-1-mutation-track` | [`polish-1-mutation-track.md`](polish-1-mutation-track.md) | cloud | `.github/workflows/{ci,mutation}.yml`, 6× `packages/<pkg>/stryker.conf.json`, `README.md` (1 line) | Pulls mutation out of every-PR CI into `workflow_dispatch`; adds Stryker config to 6 packages so the on-demand surface is comprehensive. |
| `polish-1-property-track` | [`polish-1-property-track.md`](polish-1-property-track.md) | cloud | 6 new `*.properties.test.ts` files + 1 extension across 5 packages | fast-check on every major package; pairs with mutation track (mutation finds dead conditionals; properties verify the boundaries). |

These two have no shared files and no dep edge → safe to parallel. The cursor-runner / store / workflow / mcp packages get touched by both, but at the package root (`stryker.conf.json`) vs `src/` subtree (`*.properties.test.ts`) respectively — textually disjoint, no rebase risk.

### Batch 2 — after batch 1 (README rewrite)

| Stream | Spec | Runtime | Touches | Why |
|---|---|---|---|---|
| `polish-1-readme-track` | [`polish-1-readme-track.md`](polish-1-readme-track.md) | cloud | `README.md` (rewrite) + 6 new package READMEs + `core` / `workflow` refresh | Full README polish; depends on mutation track's `mutation.yml` so the top-level README's mutation-invocation line points at a real workflow. |

Soft dep on `polish-1-mutation-track` (`mutation.yml` reference) and hard overlap on `README.md` (the 1-line mutation-track addition gets subsumed by the rewrite). Either way, readme track runs after batch 1 lands so the rewrite picks up batch 1's state cleanly.

## Conflict scan summary

- **File overlap**: 1 hard overlap (`README.md` between mutation + readme tracks; resolved by serial order). 6 soft overlaps (mutation + property both touch various `packages/<pkg>/` dirs but at disjoint subpaths — package root vs `src/` subtree).
- **Dep signals**: 1 explicit edge (readme → mutation).
- **No deps found between**: mutation ↔ property.

## Runtime selection

All 3 streams fire cloud. Operator directive (*"run this all in the cloud using our ship tooling"*) is the override; no local-force signals applied. Cursor cloud picks each PR's branch name automatically (no `branch_name:` field in cloud streams) and opens the PR via `autoCreatePR=true`.

## Why batches not just one driver run

Batch 1 fires both streams in parallel and waits for both to merge before batch 2 starts. The dep edge in the frontmatter tells `/work-driver` this; no manual pausing required. After batch 1 merges, `/work-driver` fast-forwards readme-track against the new `origin/main` (with the mutation track's `mutation.yml` + 1-line README hint already in place), then fires batch 2.

## Manual override

If you need to fire batch 1's streams sequentially instead of in parallel (e.g. to keep cloud minute spend down), invoke per-batch with the stream count restriction implicit:

```
/work-driver docs/features/polish-round-1/driver.md --batch 1
```

The driver will read the manifest, fire both batch-1 streams in parallel by default. To force single-stream-at-a-time, edit `streams[]` in batch 1 to put one stream per batch (split batch 1 into 1a + 1b with the same `depends_on: []`).
